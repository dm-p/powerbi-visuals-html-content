// Public API for the landing-page feature. The visual only needs the handler;
// the splash builder, icons, types, and generated mark are internal.
export { default as LandingPageHandler } from './handler';
